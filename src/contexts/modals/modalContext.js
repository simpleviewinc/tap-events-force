import React from 'react'

/**
 * Creates the ModalContext for accessing the modal component
 * @param {Object} ModalComponent - Component to use for whosing modals
 */
export const ModalContext = React.createContext(null)
