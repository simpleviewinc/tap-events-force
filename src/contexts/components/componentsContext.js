import React from 'react'

/**
 * Creates the ComponentsContext for accessing the modal component
 * @param {Object} Components - Group of Component passed to the Session component as props
 */
export const ComponentsContext = React.createContext(null)
