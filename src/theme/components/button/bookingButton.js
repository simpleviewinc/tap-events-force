import { colors } from '../../colors'

const pendingStyles = {
  pending: {
    text: {
      color: colors.lightGray,
    },
    icon: {
      color: colors.lightGray,
    },
  },
}

export const bookingButton = {
  ...pendingStyles,
}
