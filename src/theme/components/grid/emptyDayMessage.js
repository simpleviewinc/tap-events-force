import { colors } from '../../colors'

export const emptyDayMessage = {
  main: {
    $xsmall: {
      ftSz: 18,
      alS: 'center',
      txAl: 'center',
      c: colors.gray,
    },
    $small: {
      ftSz: 20,
      mT: 40,
    }
  },
}
