import { colors } from '../colors'

export const stateLabel = {
  main: {
    flD: 'row',
    alI: 'center',
    mR: 15
  },
  stateText: {
    ftSz: 12,
    ftWt: 500,
    c: colors.primary
  },
  check: {
   fill: colors.primary,
   maxH: 18,
  }
}