import { colors } from '../colors'

export const stateLabel = {
  main: {
    flexDirection: 'row',
    alI: 'center',
    mR: 15
  },
  stateText: {
    ftSz: 12,
    ftWt: 500,
    color: colors.primary
  },
  check: {
   fill: colors.primary,
   maxHeight: 18,
  }
}